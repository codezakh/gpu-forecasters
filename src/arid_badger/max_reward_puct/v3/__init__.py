"""v3 max-reward PUCT: phased flow with surrogate forecasting.

See ZAI-86 for the design. Top-level surface: ``SearchDriver``,
``SearchConfig``, ``SearchState``, the ``SearchEvent`` discriminated
union, and the dispatch types.
"""
