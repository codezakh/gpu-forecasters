"""Concrete mutation providers implementing `hill_climbing.domain.MutationProvider`.

Mirrors the layout of `hill_climbing.scoring_providers/`: the domain protocol
lives one directory up, and per-domain implementations sit here. Providers in
this package are shared by both hill-climbing and max-reward-PUCT stacks.
"""
